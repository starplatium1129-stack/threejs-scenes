# 新场景约定

## 独立目录

使用简短的英文短横线目录名，例如 `hakurei-shrine/`。新人物或独立世界使用新的同级目录，避免覆盖现有作品。每个场景自行声明依赖并提交锁文件，能独立安装、运行与构建。

## 每个场景应包含

- `README.md`：场景简介、交互、运行与构建步骤。
- `RECREATE_PROMPT.md`：合并最终要求的完整提示词，不依赖聊天记录。
- `src/`：模块化场景、材质、人物、动画逻辑。
- `public/assets/`：所有运行必需素材及来源 / 生成提示词；避免引用个人电脑外部路径。
- `previews/`：当前最终截图，必要时附一段动作预览；不混入过期截图。
- `offline/`：适合非技术用户双击的交付版和简单说明。
- `tests/`：必要的渲染、交互与动画验证。
- 构建脚本、依赖声明与锁文件。

## 提交与发布

1. 完成场景和必要验证，更新预览与完整提示词。
2. 如提供离线版本，将程序与素材完整嵌入 HTML，并验证 `file://` 与断网运行。
3. 更新根目录 README 的场景目录。
4. 不提交依赖目录、缓存、测试临时输出或本地密钥。
5. 用户直接体验的压缩包放在 GitHub Releases，避免在 Git 历史中重复保存压缩包。

## 保持可还原

生成式素材同时保留原图和生成提示词。准确恢复依赖源代码、锁文件与素材；文字提示词用于重新制作相近效果，不保证完全相同。
